export default async function ({ app, store, redirect }) {
  //   const isAuthenticated = store.getters['auth-store/isAuthenticated']
  //   const user = store.getters['auth-store/user']
  //   if (!isAuthenticated && !Object.keys(user).length) {
  //     await store.dispatch('auth-store/refreshUser')
  //   }
  //   if (Object.keys(store.getters['profile-store/currentUser']).length === 0) {
  //     app.$axios.setToken(token, 'Bearer')
  //     try {
  //       await store.dispatch('auth-store/refreshUser')
  //     } catch (error) {}
  //   }
}
